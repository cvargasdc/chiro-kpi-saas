/**
 * ChiroTouch / SimplePractice / OpenAI mapping remain out of this rebuild.
 * Generic CSV/Excel import lives in `server/import/http.ts`.
 */
export const IMPORT_OUT_OF_SCOPE = {
  openai: false,
  chirotouch: false,
  simplepractice: false,
} as const;
